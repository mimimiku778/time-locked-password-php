# Deny all files by default
Order deny,allow
Deny from all

# Allow only specific files
<Files "index.php">
    Allow from all
</Files>

# Allow only safe asset files
<FilesMatch "\.(css|js|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot)$">
    Allow from all
</FilesMatch>